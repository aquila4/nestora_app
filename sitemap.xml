from flask import Response
from app.models import Property  # adjust if needed

@app.route("/sitemap.xml")
def sitemap():
    base_url = "https://www.usenestora.com"

    properties = Property.query.all()

    urls = [
        f"{base_url}/",
        f"{base_url}/home",
        f"{base_url}/list-property/step1",
        f"{base_url}/lagos-properties",
        f"{base_url}/abuja-properties",
    ]

    for p in properties:
        urls.append(f"{base_url}/property/{p.id}")

    xml = ['<?xml version="1.0" encoding="UTF-8"?>']
    xml.append('<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">')

    for url in urls:
        xml.append(f"""
        <url>
            <loc>{url}</loc>
            <changefreq>weekly</changefreq>
            <priority>0.8</priority>
        </url>
        """)

    xml.append("</urlset>")

    return Response("\n".join(xml), mimetype="application/xml")