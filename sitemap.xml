@app.route("/sitemap.xml")
def sitemap():
    properties = Property.query.filter_by(approved=True).all()

    urls = []

    # STATIC PAGES
    urls.append("https://www.usenestora.com/")
    urls.append("https://www.usenestora.com/home")
    urls.append("https://www.usenestora.com/list-property/step1")

    # DYNAMIC PROPERTY PAGES (SEO SLUG)
    for p in properties:
        if p.slug:
            urls.append(f"https://www.usenestora.com/property/{p.slug}")

    xml = """<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
"""

    for url in urls:
        xml += f"""
    <url>
        <loc>{url}</loc>
        <changefreq>daily</changefreq>
        <priority>0.8</priority>
    </url>
"""

    xml += "\n</urlset>"

    return Response(xml, mimetype="application/xml")